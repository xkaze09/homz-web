import Section from "@/components/layout/Section";

export default function PaymentPage() {
  return (
    <main>
      <Section>
        <h1 className="text-2xl font-semibold">Payment</h1>
        <p className="mt-2 text-sm opacity-80">
          Stripe flow placeholder (not wired yet).
        </p>
      </Section>
    </main>
  );
}
